package com.example.dailyquizetest.presentation.navigation

interface WelcomeRouteProvider {
    fun route(): Route
}

interface HistoryRouteProvider {
    fun route(): Route
}

interface FiltersRouteProvider {
    fun route(): Route
}

interface QuizRouteProvider {
    fun route(): Route
}
