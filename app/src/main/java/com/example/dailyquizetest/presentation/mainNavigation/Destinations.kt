package com.example.dailyquizetest.presentation.mainNavigation

interface WelcomeRouteProvider {
    fun route(): Route
}

interface HistoryRouteProvider {
    fun route(): Route
}

interface QuizRouteProvider {
    fun route(): Route
}